upstream app {
    ip_hash;
    server app:8000;
}

server {
    listen 8000;
    server_name localhost;
    location /static/ {
      autoindex on;
      alias /sso/collected_static/;
    }

    location / {
      proxy_pass http://app/;
    }
}